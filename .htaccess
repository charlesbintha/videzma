RewriteEngine On

# Rediriger vers /public
RewriteCond %{REQUEST_URI} !^/public/
RewriteRule ^(.*)$ public/$1 [L]

# Sécurité : bloquer accès aux dossiers Laravel
RedirectMatch 403 ^/(app|bootstrap|config|database|resources|routes|storage|vendor|tests)/
